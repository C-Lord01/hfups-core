# HFUPS Protocol Core

## Setup and run (Windows)

```powershell
python -m pip install -e .
python -m hfups.cli_demo
hfups-demo
```

## Run tests

```powershell
python -m pytest
```

Or, if you use Make:

```bash
make test
```
